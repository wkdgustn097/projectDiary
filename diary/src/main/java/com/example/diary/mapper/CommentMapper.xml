<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.diary.mapper.CommentMapper">
	<select id="selectComment"  parameterType="java.util.Map" resultType="java.util.List">
		SELECT comment_no commentNo, 
			   notice_no noticeNo, 
			   member_id memberId, 
			   is_secret isSecret, 
			   comment, 
			   createdate, 
			   updatedate 
	    FROM comment 
	    WHERE notice_no = #{noticeNo} LIMIT #{beginRow},#{rowPerPage};
	</select>
</mapper>